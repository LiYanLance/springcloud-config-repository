server:
  port: 8088

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/todo_m
    username: root
    password: 123swl
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    show-sql: true

user-service:
  ribbon:
    listOfServers: localhost:8082

zuul:
  ignored-services: "*"
  sensitive-headers:
  routes:
    user:
      path: /users/**
      serviceId: user-service
      stripPrefix: false
